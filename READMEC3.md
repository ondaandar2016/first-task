# Закупка
Закупка перед раундом.

## Прайсы
| Оружие, как пишут |	Цена в голде |
|-------------------|--------------|
| G22	              |300           |
| AKR  	            |2700          |
| M4	              |3100          |
| AWM  	            |4750          |

## Шаблон. Скопировать целиком
```py
def price_of(gun):
    if gun == "G22":
        return 300
    if gun == "AKR":
        return ____
    if gun == "M4":
        return ____
    if gun == "AWM":
        return ____
    return None

try:
    gold = int(input("Голда: "))
except ValueError:
    print("Голда — число")
    raise SystemExit
gun = input("Оружие: ")
price = price_of(gun)
if price is None:
    print(________________)
elif gold < price:
    need = ________________
    print("Не хватит. Нужно ещё", need)
else:
    left = ________________
    print("Купил", gun + ". Остаток", left)
```
# Т3. Алгоритм по строкам
01 Спрашивают голду. Если это не целое число — Голда — число и программа заканчивается.
02 Спрашивают оружие. Буквы как в прайсе: G22, AKR, M4, AWM.
03 Функция price_of возвращает цену или None, если оружия нет.
04 Если None — печатают Нет такого оружия.
05 Если голды меньше цены — считают, сколько не хватает: цена минус голда.
06 Печатают Не хватит. Нужно ещё и это число.
07 Если голды хватает — остаток = голда минус цена.
08 Печатают Купил ИМЯ. Остаток ЧИСЛО
09 Точка после имени оружия обязательна. Пробелы как в эталоне.

| Ввод              |	Консоль                 |
|-------------------|-------------------------|
| 800	              |Голда: 300               |
| AKR  	            |Оружие: AKR              |
|                   |Не хватит. Нужно ещё 1900|
|-------------------|-------------------------|
| 4000	            |Голда: 4000              |
| AKR  	            |Оружие: AKR              |
|   	              |Купил AKR. Остаток 1300  |
|-------------------|-------------------------|
| 4750	            |Голда: 4750              |
| AWM  	            |Оружие: AWM              |
|   	              |Купил AKR. Остаток 0     |
|-------------------|-------------------------|
| 800 	            |Голда: 800               |
| нож  	            |Оружие: нож              |
|   	              |Нет такого оружия        |
|-------------------|-------------------------|
| абв               |Голда: абв               |
|     	            |Голда - число            |
|   	              |                         |
|-------------------|-------------------------|

## Рабочий код
```py
    
def price_of(gun):
    if gun == "G22":
        return 300
    if gun == "AKR":
        return 2700
    if gun == "M4":
        return 3100
    if gun == "AWM":
        return 4750
    return None

try:
    gold = int(input("Голда: "))
except ValueError:
    print("Голда — число")
    raise SystemExit
gun = input("Оружие: ")
price = price_of(gun)
if price is None:
    print("Нет такого оружия")
elif gold < price:
    need = price - gold
    print("Не хватит. Нужно ещё", need)
else:
    left = gold - price
    print("Купил", gun + ". Остаток", left)
```
## Роли
Программист: Игошин Е.А.

Ревьюер: Игошин Е.А.






